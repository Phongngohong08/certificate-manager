# Example environment variables for blockchain-academic-certificates

# MongoDB
MONGODB_URI=mongodb://localhost:27017/academic-certificates

# Express session secret
EXPRESS_SESSION_SECRET=your-session-secret

# JWT secret
JWT_SECRET=your-jwt-secret

# Fabric network
CCP_PATH=absolute/path/to/connection-profile.json
FABRIC_CHANNEL_NAME=mychannel
FABRIC_CHAINCODE_NAME=academic-certificates

# Frontend URL (for CORS in production)
FRONTEND_URL=http://localhost:3000

# Node environment
development
NODE_ENV=development
