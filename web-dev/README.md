# Web Dev (Node.js + ReactJS)

- `server/`: Backend Node.js/Express
- `client/`: Frontend ReactJS

## Cách chạy

1. Vào `server/` chạy: `npm install && npm start`
2. Vào `client/` chạy: `npm install && npm start`
